# Scripts

## Validate

Run the repo's validation target:

```bash
./scripts/validate.sh
```

The script defaults to `F23.StringSimilarity.sln` with `CONFIGURATION=Release`. Override `VALIDATION_TARGET` or pass extra `dotnet test` arguments when needed.
